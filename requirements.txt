# Pinned 23/09/2026 (audit): BASELINE_0 reproduced bit-for-bit with exactly these
# versions. Loose ">=" let every nightly run pick up new releases and silently
# change backtest numbers. Upgrade deliberately: bump, rerun, compare trades.
requests==2.33.1
numpy==2.2.6
pandas==2.3.3
# vnstock/vnai: PyPI go het ban (25/09/2026) — bootstrap.py goi thang VCI.
certifi==2026.04.22
